.idea
node_modules
dist
.astro
playwright-report
test-results

# Local-only: iteration plans, agent notes, drafts (not committed)
.local/

# Agent-only database, scripts, and raw sources — not used by Astro build
ai-agents/**

# Python cache from agent scripts (if present locally)
__pycache__/
*.pyc
