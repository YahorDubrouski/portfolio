---
import OutcomesPage from '../../../templates/OutcomesPage.astro';
import { getSiteTranslations } from '../../../i18n/translations/site';
const locale = 'en';
const t = getSiteTranslations(locale);
---
<OutcomesPage locale={locale} t={t} role="devops" />
